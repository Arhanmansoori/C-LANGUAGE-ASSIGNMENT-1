#include<stdio.h>
#include<conio.h>
void main()
{

    char str[90];
    int i;
    printf("enter the string : \n");
    scanf("%s",&str);
    printf("the string is %s",str);
    printf("the length of string is %d",strlen(str));
    getch();

}
